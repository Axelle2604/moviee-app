import React, { memo } from 'react';
import { FooterContainer } from './styled-components/footerStyled';

const Footer = () => {
  return <FooterContainer>Axelle DRU - 2019</FooterContainer>;
};

export default memo(Footer);
